<h4 class="my-4">Add a review to <strong><%= @restaurant.name %></strong></h4>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: (0..5) %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
